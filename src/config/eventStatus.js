export const REGISTRATIONS_OPEN = false;

// ─── MAINTENANCE / LOADING SCREEN TOGGLE ───────────────────────────────────
// Set to `true`  → public site shows a permanent loading screen (event is over)
// Set to `false` → public site works normally again
// Admin routes (/admin/*) are NEVER affected by this flag.
export const SITE_MAINTENANCE_MODE = true;

export const REGISTRATION_CLOSED_MESSAGE =
  'Registrations, submissions, and payment confirmations are now closed as Lumiere 2026 has concluded.';

export const REGISTRATION_CLOSED_SHORT_MESSAGE = 'Registrations are now closed.';